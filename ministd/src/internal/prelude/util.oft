(module ministd/internal/prelude/util
  [debug-trace shl?]
  no-prelude)

(import ministd/internal/prelude/intrinsics
  [car cons? eq false writeln])

(intrinsics:defn debug-trace (x)
  (writeln x)
  x)

(intrinsics:defn shl? (sym value)
  (if (cons? value)
    (eq (car value) sym)
    false))
