(module main
  [main])

(import oftb-macro-expander
  say-hello)
(import oftb-macro-expander/sub/mod
  say-hello-from-sub-mod)

(defn main (args)
  (if (not (= (length args) 1))
    (panic "usage: oftb-macro-expander package-dir"))
  ; (todo)
  (println 'todo)

  (say-hello)
  (say-hello-from-sub-mod))
