(module macro-expander/interpreter/env [default-env])

(def default-env (list (cons (quote 0?) 0?) (cons (quote 1+) 1+) (cons (quote 1-) 1-) (cons (quote either) either) (cons (quote left) left) (cons (quote left?) left?) (cons (quote right) right) (cons (quote right?) right?) (cons (quote append) append) (cons (quote assoc) assoc) (cons (quote each) each) (cons (quote flat-map) flat-map) (cons (quote foldl) foldl) (cons (quote foldr) foldr) (cons (quote length) length) (cons (quote map) map) (cons (quote nth) nth) (cons (quote partition) partition) (cons (quote reverse) reverse) (cons (quote skip) skip) (cons (quote take) take) (cons (quote list<-vector) list<-vector) (cons (quote string<-symbol) string<-symbol) (cons (quote symbol<-string) symbol<-string) (cons (quote and) and) (cons (quote not) not) (cons (quote or) or) (cons (quote string-append) string-append) (cons (quote string-length) string-length) (cons (quote string-nth) string-nth) (cons (quote string-slice) string-slice) (cons (quote expect) expect) (cons (quote none) none) (cons (quote none?) none?) (cons (quote some) some) (cons (quote some?) some?) (cons (quote fst) fst) (cons (quote map-fst) map-fst) (cons (quote map-pair) map-pair) (cons (quote map-snd) map-snd) (cons (quote pair) pair) (cons (quote snd) snd) (cons (quote vector-append) vector-append) (cons (quote vector-each) vector-each) (cons (quote vector-length) vector-length) (cons (quote vector-nth) vector-nth) (cons (quote vector-slice) vector-slice) (cons (quote *) *) (cons (quote +) +) (cons (quote -) -) (cons (quote =) =) (cons (quote byte?) byte?) (cons (quote bytes?) bytes?) (cons (quote car) car) (cons (quote cdr) cdr) (cons (quote cons) cons) (cons (quote cons?) cons?) (cons (quote eq) eq) (cons (quote equals) equals) (cons (quote false) false) (cons (quote fixnum?) fixnum?) (cons (quote function?) function?) (cons (quote list) list) (cons (quote nil) nil) (cons (quote nil?) nil?) (cons (quote panic) panic) (cons (quote print) print) (cons (quote println) println) (cons (quote string?) string?) (cons (quote symbol?) symbol?) (cons (quote true) true) (cons (quote vector?) vector?) (cons (quote write) write) (cons (quote write-bytes) write-bytes) (cons (quote writeln) writeln)))
